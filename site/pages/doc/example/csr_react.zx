pub fn Page(allocator: zx.Allocator) zx.Component {
    visit_count += 1;

    return (
        <main @allocator={allocator}>
            <CounterComponent @rendering={.csr} visit_count={visit_count} />
        </main>
    );
}

var visit_count: u64 = 0;
const zx = @import("zx");
const CounterComponent = @jsImport("./csr_react.tsx");
