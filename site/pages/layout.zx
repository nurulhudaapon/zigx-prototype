pub fn Layout(ctx: zx.LayoutContext, children: zx.Component) zx.Component {
  
  return (
    <html lang="en-US" @allocator={ctx.arena}>
      <head>
        <title>Zx</title>
      </head>

      <body>
        <template shadowrootmode="open">
          {(children)}
        </template>
      </body>
    </html>
  );
}

const zx = @import("zx");
const std = @import("std");