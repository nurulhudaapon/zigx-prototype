name: "zx"
services:
  doc_site:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: "ziex.dev"
    restart: unless-stopped
    networks:
      - npm

networks:
  npm:
    driver: bridge
