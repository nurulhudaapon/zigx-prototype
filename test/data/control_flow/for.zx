pub fn Page(ctx: zx.PageContext) zx.Component {
    const user_names = [_][]const u8{ "John", "Jane", "Jim", "Jill" };
    return (
        <main @allocator={ctx.arena}>
            {for (user_names) |name| (<p>{name}</p>)}
        </main>
    );
}

const zx = @import("zx");
