pub fn Page(allocator: zx.Allocator) zx.Component {
    const is_logged_in = true;
    return (
        <main @allocator={allocator}>
            {if (is_logged_in) (
                <div>
                    <p>Welcome, User</p>
                </div>
            )}
        </main>
    );
}

const zx = @import("zx");
